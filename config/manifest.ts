import type { UserManifest } from "wxt";

export default {
  name: "GP Books Extended",
  description:
    "Extension to add features to Google Play Books, such as page animations, bubble zoom, book theme etc.",
  version: "0.1",
  permissions: ["storage"],
} satisfies UserManifest;
